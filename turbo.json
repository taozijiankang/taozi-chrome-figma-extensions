{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "package": {
      "dependsOn": ["@taozi-chrome-figma-extensions/mcp-server#build", "@taozi-chrome-figma-extensions/browser-plugin#build"],
      "cache": false
    }
  }
}
